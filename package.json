{
  "name": "roster-converter",
  "private": true,
  "packageManager": "pnpm@11.22.0",
  "scripts": {
    "postinstall": "node scripts/sync-api.mjs",
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "sync:api": "node scripts/sync-api.mjs"
  },
  "devDependencies": {
    "turbo": "^2.1.0"
  }
}
